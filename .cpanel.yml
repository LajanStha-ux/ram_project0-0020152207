git checkout -- .cpanel.yml
```

Then open `.cpanel.yml` in Notepad, make sure content is:
```
---
deployment:
  tasks:
    - export DEPLOYPATH=/home2/hjnblcom/public_html/myapp
    - /usr/local/bin/npm install