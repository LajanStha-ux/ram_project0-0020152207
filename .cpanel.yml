---
deployment:
  tasks:
    - export DEPLOYPATH=/home2/hjnblcom/public_html/myapp
    - /usr/local/bin/node -v
    - /usr/local/bin/npm install
    - /usr/local/bin/npm run build
    - cp -r $DEPLOYPATH/public/. $DEPLOYPATH
